// setting a type ...
let greeting:string = 'hello'
// ... makes it impossible to assign a value of a different type
// ... makes the methods and properties of the type available
console.log(greeting.toUpperCase())
console.log(greeting.toLowerCase())
console.log(greeting.endsWith('o'))
// ... and prevents using methods and properties of a different type
// console.log(greeting.toFixed(2))
